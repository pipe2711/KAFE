def raiseDifferentDimension(funcion):
    raise Exception(f"{funcion}: Matrices don't have the same dimension")

def raiseNonUniformMatrix(funcion):
    raise Exception(f"{funcion}: Matrix is not uniform")
