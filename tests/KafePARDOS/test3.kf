import pardos;

PARDOS df = pardos.read_csv("test3.csv");

PARDOS all = df;
show(all);

show(df.shape());

List[STR] ciudades = df.col("city");
show(ciudades);

show(df.dtypes());

show(df.info());

PARDOS desc = df.describe();
show(desc);
