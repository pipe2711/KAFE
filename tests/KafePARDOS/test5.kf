import pardos;

-- Intentar leer un CSV vacío
PARDOS df = pardos.read_csv("test5.csv");

-- Mostrar DataFrame resultante (sin columnas ni filas)
PARDOS all = df;
show(all);

-- Shape (debe ser [0,0])
show(df.shape());

-- dtypes (lista vacía)
show(df.dtypes());

-- Info general
show(df.info());

-- Describe (solo cabecera)
PARDOS desc = df.describe();
show(desc);
