import pardos;

-- Leer CSV con solo cabecera
PARDOS df = pardos.read_csv("test6.csv");

-- Mostrar DataFrame (solo cabecera)
PARDOS all = df;
show(all);

-- Shape (debe ser [0,3])
show(df.shape());   -- [0, 3]

-- dtypes (todas STR, ya que no hay filas numéricas)
show(df.dtypes());

-- Info general
show(df.info());

-- Describe (solo cabecera)
PARDOS desc = df.describe();
show(desc);
