import pardos;

PARDOS df = pardos.read_csv("test9.csv");

PARDOS all = df;
show(all);

show(df.shape());

List[FLOAT] v1 = df.col("valor");
show(v1);

List[FLOAT] v1_again = df.col("valor");
show(v1_again);

show(df.dtypes());

show(df.info());

show(df.describe());

show(df.col("z"));
