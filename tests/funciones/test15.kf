List[INT] numeros = [1, 2, 3];
List[STR] letras = ["a", "b"];
List[BOOL] flags = [True];
List[List[List[STR]]] cadenas = [];

show(numeros);
show(cadenas);

append(numeros, 99);

append(letras, "z");
append(flags, False);
append(cadenas, [[["asdf"]]]);

show(numeros);
show(letras);
show(flags);
show(cadenas);

remove(numeros, 2);
remove(letras, "a");

show(numeros);
show(letras);

show(len(numeros));
