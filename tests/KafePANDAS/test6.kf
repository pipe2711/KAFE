import pandas;

-- Leer CSV con solo cabecera
PANDAS df = pandas.read_csv("test6.csv");

-- Mostrar DataFrame (solo cabecera)
PANDAS all = df;
show(all);

-- Shape (debe ser [0,3])
show(df.shape());   -- [0, 3]

-- dtypes (todas STR, ya que no hay filas numéricas)
show(df.dtypes());

-- Info general
show(df.info());

-- Describe (solo cabecera)
PANDAS desc = df.describe();
show(desc);
