import pandas;

-- Leer el CSV con floats y celdas vacías
PANDAS df = pandas.read_csv("test2.csv");

-- Mostrar DataFrame completo (usa __repr__ para ver filas)
PANDAS all = df;
show(all);

-- Shape
show(df.shape());

List[FLOAT] valores = df.col("value");
show(valores);

-- Tipos inferidos
show(df.dtypes());

-- Info general
show(df.info());

-- Describe
PANDAS desc = df.describe();
show(desc);
